# Dependencies
node_modules/
npm-debug.log
yarn-debug.log
yarn-error.log

# Environment files
.env
.env.local
.env.*.local

# IDE files
.idea/
.vscode/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Build output
dist/
build/

# Firebase local deploy cache (must never affect git / CI sync)
.firebase/

# Firebase Admin service-account keys — NEVER commit these (full project access)
*serviceAccount*.json
*service-account*.json
*-firebase-adminsdk-*.json
scripts/github-action-gisugo1-key.json
secrets/
admin-keys/

# Local pre-surgery file snapshots (rollback safety nets) — local-only, never pushed.
# See docs/ADMIN_DASHBOARD_MOCK_REMOVAL_PLAN.md for what each backup is for.
backups/